# `mix format`
## автоматическое форматирование [[Elixir]]-кода
#mix #Elixir

```Elixir
# Used by "mix format"
[
  inputs: ["{mix,.formatter}.exs", "{config,lib,test}/**/*.{ex,exs}"]
]
```

```sh
$ mix format
```
или
```sh
$ make format
$ make tmp/format_ex
```
